# Personal data (user fills these)
data/applications.md
data/pipeline.md
data/scan-history.tsv
reports/*.md
!reports/.gitkeep
output/*
!output/.gitkeep
batch/logs/*
!batch/logs/.gitkeep
batch/batch-state.tsv
batch/batch-input.tsv
batch/tracker-additions/*.tsv
!batch/tracker-additions/.gitkeep
jds/*
!jds/.gitkeep

# User config and customization (never auto-updated)
config/profile.yml
portals.yml
modes/_profile.md
.update-dismissed
.update-lock

# Generated
.resolved-prompt-*
node_modules/

# OS
.DS_Store
*.mov
*.mp4

# Environment
.env

# Scan dedup state
data/seen-urls.json
data/signal-seen.json
data/enrichments.json

# Claude Code local
.claude/settings.local.json
.claude/memory/
career-dashboard
package-lock.json

# Working-tree artifacts — generated / machine-specific, not part of the project
.next/
logs/
batch/tracker-additions/merged/
# launchd job defs with hardcoded /Users/... paths
scripts/*.plist

# Nested reference repos kept locally — not ours to commit
forge/
hebbia-signal-engine-reference/

# Personal interview prep — gitignore before commit (will swap in examples/ template before going public)
interview-prep/hearth-gtm-engineer.md
interview-prep/hebbia-gtm-engineer.md

# Working notes (not for the repo)
docs/PHASE_11_TODO.md
WORK_LOG.md
WORK_LOG_T*.md
MORNING_REPORT.md
MORNING_REPORT_FINAL.md

# Keep dashboard-web lockfile despite the global package-lock.json ignore
!dashboard-web/package-lock.json

# autoapply (browser-harness based)
autoapply/.venv/
autoapply/__pycache__/
autoapply/**/__pycache__/
autoapply/*.log
autoapply/.env
autoapply/browser-harness/
# Real profile (PII); .example is committed as the schema source of truth.
autoapply/profile.json
# Temp fixtures created by Python unit tests
autoapply/tests/_tmp/

# ─── Analytics ────────────────────────────────────────────────────────────────
# Per-machine runtime data — high volume, per-day rotation. Never committed.
# See docs/analytics/ANALYTICS.md for details.
data/events/
data/analytics/
data/auto-promotions/
WORK_LOG_ANALYTICS.md
# Personal data dirs surfaced by symlinks in this worktree
data/briefings
data/chats
# Scan-job records + logs (Step 8 kickoff model) and per-day event JSONL
data/scans/
data/career-ops-events/*.jsonl
# ─────────────────────────────────────────────────────────────────────────────
WORK_LOG_12H.md

# Runtime artifacts
.claude/
.gstack/

# Session reports and logs (already-merged sessions)
MERGE_REPORT_*.md
WORK_LOG_*.md

# AutoApply PII protection — never commit source resume PDFs
autoapply/resumes/source/*.pdf

# Task G budget tracker — machine-local counter for Claude API spend
data/budget/

# Per-company thesis cache — local Claude output keyed by slug
data/company-theses/

# Dogfood week private notes
notes/dogfood-week.md

# PersonaLab session artifacts (regenerated on every orchestrator run)
qa/runs/
qa/reports/
qa/regressions/
qa/scenarios-results/
qa/synthesis/
personalab/__pycache__/
personalab/**/__pycache__/
qa/__pycache__/
qa/**/__pycache__/
